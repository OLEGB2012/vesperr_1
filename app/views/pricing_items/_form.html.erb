<%= form_with(model: pricing_item, local: true) do |form| %>
  <% if pricing_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pricing_item.errors.count, "error") %> prohibited this pricing_item from being saved:</h2>

      <ul>
      <% pricing_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pricing_section_id %>
    <%= form.text_field :pricing_section_id %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
