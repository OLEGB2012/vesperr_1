<%= form_with(model: feature_section, local: true) do |form| %>
  <% if feature_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feature_section.errors.count, "error") %> prohibited this feature_section from being saved:</h2>

      <ul>
      <% feature_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.text_field :section_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
