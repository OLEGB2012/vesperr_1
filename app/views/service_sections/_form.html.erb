<%= form_with(model: service_section, local: true) do |form| %>
  <% if service_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_section.errors.count, "error") %> prohibited this service_section from being saved:</h2>

      <ul>
      <% service_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.text_field :section_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
